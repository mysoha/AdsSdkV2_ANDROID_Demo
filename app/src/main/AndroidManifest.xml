<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".basic.BasicActivity" android:theme="@style/Theme.MyApplication" />
        <activity android:name=".mix.ScrollActivity" android:theme="@style/Theme.MyApplication" />
        <activity android:name=".mix.ListActivity" android:theme="@style/Theme.MyApplication"/>
        <activity android:name=".mix.VisibilityActivity" android:theme="@style/Theme.MyApplication"/>
        <activity android:name=".mix.RecyclerActivity" android:theme="@style/Theme.MyApplication"/>
        <activity android:name=".mix.RecyclerHorizontalActivity" android:theme="@style/Theme.MyApplication"/>
        <activity android:name=".mix.viewpager.ViewPagerActivity" android:theme="@style/Theme.MyApplication"/>
        <activity android:name=".mix.viewpager_test.ViewPagerTestActivity" />

        <activity android:name=".synthetic.FormActivity" android:theme="@style/Theme.MyApplication"/>
        <activity android:name=".fake.FakeActivity" android:theme="@style/Theme.MyApplication"/>

        <activity android:name=".browser.AutoFillActivity" android:theme="@style/Theme.MyApplication"/>
        <activity android:name=".browser.BrowserGameActivity" android:theme="@style/Theme.MyApplication"/>
        <activity android:name=".browser.LiveStreamActivity" android:theme="@style/Theme.MyApplication"/>
        <activity android:name=".browser.EcommerceActivity" android:theme="@style/Theme.MyApplication"/>
    </application>

</manifest>